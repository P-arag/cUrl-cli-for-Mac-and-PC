# cUrl-cli-for-Mac-and-PC
## A cUrl command cli emulator for Mac and PC
## Usage instructions
```
python curl.py -u <url, json or html> --json(default) / --not-json(necessary when you want to scrape a web-page)
```
